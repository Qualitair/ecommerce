( function ( $ ) {
	'use strict';
	
	$( function() {
		// $('.bb-products').find('li').bind('mouseover', function() {
			
		// 	var _ = $(this),
		// 	  img = _.find('img:last'),
		// 	  title = _.parent().find('h3').text(),
		// 	  position = {
		// 	  	'top' : 20,
		// 	  	'left': _.width()
		// 	  };

		// 	 var divClassName = '.product-quickview-section',
		// 	 	div = $('<div class="product-quickview-section"></div>'),
		// 	 	content = '<div><img src="[IMG]" /><p>[TITLE]<br/> <a>Show Details</a></p></div>';

		// 	if(_.find(divClassName).length) return;

		// 	 _.find('.product-inner').prepend(div);
		// 	 div.append(content.replace('[IMG]', img.src).
		// 	 	replace('[TITLE]', title));

		// 	 div.
		// 	 	css(position).
		// 	 	fadeIn();
		// }).bind('mouseout', function() {
		// 	var div = $(this).find('.product-quickview-section');

		// 	div.fadeOut( function(){
		// 		$(this).empty().remove();
		// 	})
		// });
	});

} )( jQuery );